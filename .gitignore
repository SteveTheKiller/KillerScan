# Build output
bin/
obj/
*.user

# Visual Studio
.vs/
*.suo

# Publish
publish/
*.pubxml.user

# Local notes / scratch
*.odt

# OS junk
Thumbs.db
.DS_Store

# Cloudflare CLI cache (scan-landing deploys)
.wrangler/

# Built Chocolatey packages - release.ps1 packs these on demand
choco/*.nupkg

# Heavy source art - stored locally only, not shipped and not part of the project.
# Anchored with a leading slash so it only ignores KillerScan/brand at the repo root.
/brand/
